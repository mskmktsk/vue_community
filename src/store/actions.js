export default {
  signInGithub(context, payload) {
    return new Promise((resolve, _) => {
      resolve(payload)
    })
  },
  signInGithubResult(context, payload) {
    return new Promise((resolve, _) => {
      resolve(payload)
    })
  }
}
